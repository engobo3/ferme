{
  "name": "@diaspora-trust/core-logic",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npx vitest run",
    "lint": "eslint src/"
  }
}
